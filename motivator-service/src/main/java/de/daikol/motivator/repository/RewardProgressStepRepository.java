package de.daikol.motivator.repository;

import de.daikol.motivator.model.RewardProgressStep;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RewardProgressStepRepository extends JpaRepository<RewardProgressStep, Long> {
}
